1. Будет создан 1 экземпляр на всё приложение, т.к. в сервис providedIn: 'root'

2. Будет создано 3 экземпляра:
  - 1 на всё приложение, потому что будет создан в RootInjector
  - 2 на каждый экземпляр компонента app-child, которые будут созданы в ComponentInjector

3. ChildComponent получит экземпляр из ComponentInjector (ParentComponent), потому что Angular сначала проверяет ComponentInjector, а затем RootInjector 

4. 1, т.к. один в RootInjector, а useExisting говорит: "если есть экземпляр, то бери его, и не создавай новый"

5. Будет вызвана при первом inject, т.к. если сервис нигде не используется, то angular убирает его из bundle (tree shaking)

6. Массив из 2-х значений: ['A', 'B'], т.к. multi даёт возможность накапливать экземпляры

7. Ошибки не будет, приложение запустится корректно, т.к. optional даёт возможность пропустить данную ошибку

8. Будет ошибка, потому что self заставляет искать provider в текущем компоненте, не поднимаясь выше по дереву injector'ов

9. Экземпляр будет получен из ParentComponent, т.к. skipSelf заставляет пропустить поиск provider в текущем компоненте

10. Будет ошибка, т.к. injector не поймёт, какой сервис нужно создать без рецепта (provider'a)

11. 1, т.к. мы запровайдили его в root. Angular будет искать по дереву injector'ов

12. 
  1) существует 2 экземпляра: 1 в HeaderComponentInjector, другой в RootInjector
  2) Который находится в HeaderComponentInjector
  3) Получит из RootInjector, т.к. Angular не ищет у соседей
  4) Получит из RootInjector, т.к. Angular не ищет у соседей
  5) UserCardComponentInjector > DashboardComponentInjector > AppComponentInjector > RootInjector > PlatformInjector

13. 
  1) Angular ищет provider сервиса A
  2) нашёл ? ищет экземпляр в памяти : проходит дальше по дереву injector'ов вплоть до Null
  3) экземпляр есть ? возвращает его
  4) экземпляра нет ? проверяет на наличие зависимостей в сервисе
  5) есть зависимости ? повторение алгоритма (рекурсия)
  6) возвращение экземпляра сервиса
  7) кеширование экзпляра

14. 
  1) До inject в памяти 0 экземпляров
  2) После inject в памяти 3 экземпляра, т.к. все сервисы provideIn root, и чтобы начал работать UserService нужен ApiService (1 + 1 = 2), а для ApiService нужен LoggerService (2 + 1 = 3)

15. 
  1) providedIn: 'root', т.к. может вызываться в любой точке приложения
  2) providedIn: 'root', т.к. пользователь может быть в любой точке приложения (ему доступной)
  3) providers роутинга, т.к. только на роуте /store и его дочерних маршрутах используется данный сервис
  4) providers роутинга, т.к. нужны отдельные экземпляры сервиса на разных страницах
  5) providedIn: 'root', т.к. может вызываться в любой точке приложения
  6) providedIn: 'root', т.к. нужно одно состояние на всё приложение (одна тема на всех страницах)
  7) providers компонента, т.к. используется только на одной странице
  8) providers компонента, т.к. используется только на одной странице
  9) providedIn: 'root', т.к. может использоваться любым компонентом
  10) providedIn: 'root', т.к. нужен один экземпляр на всё приложение
  11) InjectionToken, т.к. нам нужен кастомный объект, а не класс сервиса
  12) useValue, т.к. просто хранит значение
  13) useFactory, т.к. нужна динамика (зависимость от начальных настроек)